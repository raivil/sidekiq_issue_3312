== TODO
1. Update Gemfile with user/pass for sidekiq-ent

== How to Run
1. Run sidekiqswarm with 4 workers and MAXMEM_KB=30000 to force restarts.
COUNT=4 MAXMEM_KB=30000 bundle exec sidekiqswarm -q critical -q default -q medium -q low

2. Run Rails console

- Add records
model = Model.new(name: "test_01")
model.save!
1.upto(20).each {|i| model.sub_models.build(name: "submodel_#{i}").save!}

- Enqueue job
Jobs::BatchJob.perform_async(model.id)
